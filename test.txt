//import e, test; 
public singular int e = 0; //equivalent of Java's Static modifier
private const int f = 6+5^2; //equivalent of Java's Final modifier
int b; 

object struct{
    //protected bool b2 = true; 
    constructor(){
        //inline comment
        //b2 = 1; //havent gotten types working yet, so this is technically valid
    }
    
    ?? 
     * multi line documentation comment
    ??
    destructor(){

    }
}

int main(string args){
    int x = 32 - b--; 
    private int pointer location = -> x;
    char c = @location;
    int i2 = x++;
    if  @location == x  {
    }
    x++; 
    for int pointer i = 0; not (x == 20 and x >= 0); x++ {  //while(x < 20 or x > 0){ 
        bool q = true;
        if x < 20 and (x > 0 or x > 5){
            bool l = false; 
            switch x{
                case 1{
                    //print << "yeet"; 
                }
                case 2{
                    //Just do something lmao
                }
                case 3{

                }
            }
            ++c; 
        } 
    }
}