import deez, nuts;
public singular int e = 0; //equivalent of Java's Static modifier
private const int f = 1; //equivalent of Java's Final modifier
int b; 

object struct{
    //protected bool b2 = true; 
    constructor(){
        //inline comment
        //b2 = 1; //havent gotten types working yet, so this is technically valid
    }
    
    ?? 
     * multi line documentation comment
    ??
    destructor(){

    }
}

int main(string args){
    int i = 32 - b--; 
    pointer location = -> i;
    char c = @location;
    int i2 = i++;
    if ( x < 20 and x > 0 or x > 5 ) {
    }



    ??
    for(int x = 0; x < 20 and x > 0; x++){  //while(x < 20 or x > 0){ 
        bool q = true;
        if x < 20 and x > 0 or x > 5{
            bool l = false; 
            switch(x){
                case 1{
                    //print << "yeet"; 
                }
                case 2{
                    //Just do something lmao
                }
                case 3{

                }
            }
            ++c; 
        } 
    }
    ??
}